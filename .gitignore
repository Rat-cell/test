# Campus Locker System v2.1.1 - Collaborative Git Ignore Configuration
# Only excludes temporary files and runtime data - keeps everything needed for collaboration

# === Python Cache (regenerated automatically) ===
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# === Personal Python Config ===
.python-version

# === Flask ===
instance/
.webassets-cache
.env

# === Runtime Database Files (created when app runs) ===
databases/*.db
databases/*.db-*
campus_locker_system/databases/*.db
campus_locker_system/databases/*.db-*
campus_locker_system/instance/*.db
*.db
*.sqlite
*.sqlite3

# === Runtime Log Files (created when app runs) ===
logs/*.log*
campus_locker_system/logs/*.log*
*.log
*.log.*

# === Personal Environment Variables ===
.env.local
.env.development.local
.env.test.local
.env.production.local

# === Testing Cache ===
.pytest_cache/
.coverage
htmlcov/

# === IDE Personal Settings ===
.vscode/settings.json
.idea/workspace.xml
.idea/tasks.xml
*.swp
*.swo

# === macOS Personal Files ===
.DS_Store
.AppleDouble
.LSOverride
Icon

# === Docker ===
.dockerignore

# === Node.js (if used for frontend tools) ===
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# === Temporary Files ===
*.tmp
*.temp
.tmp/
.temp/
cookies.txt
debug_*.py

# === Backup Files ===
*.bak
*.backup
*~

# === SSL Certificates ===
ssl/
*.pem
*.crt
*.key

# === Cache ===
.cache/
*.cache

# === OS ===
.DS_Store?
ehthumbs.db
Thumbs.db

# === Docker Volumes (runtime data) ===
docker-volumes/

# === Virtual Environment ===
venv/
env/
ENV/

# === Keep These Important Files for Collaboration ===
# .env (template with defaults)
# Dockerfile*
# docker-compose*.yml
# requirements.txt
# All source code
# All documentation
# All scripts
# All configuration files 