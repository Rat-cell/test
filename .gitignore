# Campus Locker System v2.1.3 - Collaborative Git Ignore Configuration
# Only excludes temporary files and runtime data - keeps everything needed for collaboration

# === Python Cache (regenerated automatically) ===
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# === Personal Python Config ===
.python-version

# === Flask ===
instance/
.webassets-cache
.env

# === Runtime Database Files (created when app runs) ===
# IMPORTANT: Include configuration JSON files but exclude runtime databases
databases/*.db
databases/*.db-*
databases/*backup*.db
campus_locker_system/databases/*.db
campus_locker_system/databases/*.db-*
campus_locker_system/databases/*backup*.db
campus_locker_system/instance/*.db
*.db
*.sqlite
*.sqlite3

# === Include Important Configuration Files ===
!databases/*.json
!campus_locker_system/databases/*.json
!lockers-hwr.json
!lockers.json
!lockers-*.json

# === Runtime Log Files (created when app runs) ===
logs/*.log*
campus_locker_system/logs/*.log*
*.log
*.log.*

# === Personal Environment Variables ===
.env.local
.env.development.local
.env.test.local
.env.production.local

# === Testing Cache ===
.pytest_cache/
.coverage
htmlcov/
.tox/

# === IDE Personal Settings ===
.vscode/settings.json
.idea/workspace.xml
.idea/tasks.xml
*.swp
*.swo

# === macOS Personal Files ===
.DS_Store
.AppleDouble
.LSOverride
Icon

# === Docker Runtime Data ===
.dockerignore

# === Node.js (if used for frontend tools) ===
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# === Temporary Files ===
*.tmp
*.temp
.tmp/
.temp/
cookies.txt
debug_*.py

# === Backup Files (exclude automatic backups but keep configuration) ===
*.bak
*.backup
*~
*backup*.db

# === SSL Certificates ===
ssl/
*.pem
*.crt
*.key

# === Cache ===
.cache/
*.cache

# === OS ===
.DS_Store?
ehthumbs.db
Thumbs.db

# === Docker Volumes (runtime data) ===
docker-volumes/
app_logs/
app_databases/
redis_data/
app_data/

# === Virtual Environment ===
venv/
env/
ENV/

# === Keep These Important Files for Collaboration ===
# Documentation: README.md, QUICK_START.md, LOCKER_OPERATIONS_GUIDE.md, DATABASE_DOCUMENTATION.md
# Configuration: docker-compose*.yml, Dockerfile*, requirements.txt, *.json configs
# Source code: All Python files, templates, static files
# Scripts: All setup and deployment scripts
# Testing: All test files and configuration

# === v2.1.3 Specific Exclusions ===
# Exclude runtime databases but keep configuration JSON files
# Exclude Docker volume data but keep compose configurations
# Exclude log files but keep log configuration 